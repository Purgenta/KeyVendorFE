import { Box } from "@chakra-ui/react";
import KeyForm from "../../../../components/sales/forms/KeyForm/KeyForm";
const index = () => {
  return (
    <Box>
      <KeyForm error="" onSubmit={() => {}} mode="edit"></KeyForm>
    </Box>
  );
};

export default index;
